iloc는 행번호를 이용 하여 key값을 가져옴
loc를 사용하여 인덱싱 > s.loc["월"],s.loc["화"],s.loc["수"],혁식으로 표현
iloc[행],loc[index]
슬라이싱 >> S.iloc[시작행번호:끝행번호] 끝 행번호 값은 미포함
슬라이싱2 >> S.loc[시작인덱스:끝인덱스] 끝 인덱스 값을 포함
시리즈에선 연속되지 않은 값을 한 번에 인덱싱이 가능하다.
s.iloc[ [0,2] ]
S.loc[ ["인덱스1","인덱스3"] ]
